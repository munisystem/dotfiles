# Font settings (WezTermと同じ)
font-family = "Monaspace Neon"
font-size = 16

# Font features (リガチャ・スタイルセット)
font-feature = calt
font-feature = liga
font-feature = dlig
font-feature = ss01
font-feature = ss02
font-feature = ss03
font-feature = ss04
font-feature = ss05
font-feature = ss06
font-feature = ss07
font-feature = ss08

# Color scheme (iceberg-dark)
theme = Iceberg Dark

# Scrollback
scrollback-limit = 5000

# Window
window-padding-x = 8
window-padding-y = 8

# Cursor
cursor-style = block
cursor-style-blink = false

# Mouse
mouse-hide-while-typing = true

# Shell integration
shell-integration = zsh
shell-integration-features = sudo

# macOS specific
macos-option-as-alt = true
